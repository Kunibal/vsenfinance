<!--グラフを変更するためのボタン集-->
<%= form_with url: ec_filter_path, local: false, method: :get do |f| %>
  <%= f.submit "月間売上" %>
  <%= f.submit "年間売上" %>
<% end %>
<br>

<div id="ec_filter_result">
  <%= render 'revenues/ec_filter_date', chartdata: @chartdata %>
</div>