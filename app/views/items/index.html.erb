<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item) do %>
      <%= image_tag item.item_image, size: "200x200" %>
    <% end %>
  </div>
  <div>
    <%= item.name %>
  </div>
  <div>
    <%= item.description %>
  </div>
  <div>
    <%= item.add_tax %>
  </div>
<% end %>